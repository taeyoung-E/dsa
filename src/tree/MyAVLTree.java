package tree;

/*
A tree that makes sure the tree is balanced

E.g From the root, 10 -> 20 -> 30 -> 40 This will result in O(n) Because all the values are shifted to the right
resulting in degenerate tree
 */
public class MyAVLTree {
}
